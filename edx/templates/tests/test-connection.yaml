apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edx.fullname" . }}-test-connection"
  labels:
    {{- include "edx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
